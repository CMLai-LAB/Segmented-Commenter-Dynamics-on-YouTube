numpy==2.3.3
matplotlib==3.10.7

# Required only when re-running model inference in
# validate_emotion_model_on_samples.py. The original inference environment
# was not version-locked.
torch
transformers
